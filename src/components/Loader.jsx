import React from "react";
import { Spin } from "antd";

const Loader = () => {
  return (
    <div className="loadernpm install -g firebase-tools">
      <Spin />;
    </div>
  );
};

export default Loader;
